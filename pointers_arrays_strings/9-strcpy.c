#include "main.h"

/**
 * _strcpy - copies a pointed string to the buffer of another pointer
 * @dest: destination
 * @src: source
 * Return: dest
 */

char *_strcpy(char *dest, char *src)
{
	*dest = *src;
	return (dest);
}
